# Dependências
node_modules

# Local Netlify folder
.netlify

# Variáveis de ambiente locais (chaves de API, segredos)
.env
.env.local
.env.*.local

# Artigo acadêmico e slides (não publicar no site — publish = ".")
ARTIGO/
